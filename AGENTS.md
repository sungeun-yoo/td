# AGENTS.md: 개발 가이드라인

이 프로젝트는 특정 객체 지향 방법론과 데이터 기반 설계를 따릅니다. 코드를 수정하거나 새로운 기능을 추가할 때, 반드시 아래의 구조와 원칙을 준수해주십시오.

## 1. 데이터 기반 설계 (Data-Driven Design)

게임의 핵심 요소들은 `js/data/` 디렉토리의 데이터 파일에 정의되어 있습니다. 새로운 콘텐츠(적, 레벨, 투사체 등)를 추가할 때는 클래스 코드를 직접 수정하는 대신 이 데이터 파일들을 수정하는 것을 원칙으로 합니다.

-   **적 데이터:** `js/data/enemy_data.js`
-   **레벨/웨이브 데이터:** `js/data/level_data.js`
-   **투사체 데이터:** `js/data/projectile_data.js`

## 2. 클래스 상속 구조

모든 게임 오브젝트는 공통 기능을 제공하는 기본 클래스(Base Class)를 상속받아 구현합니다.

-   **`BaseGameObject`**: 모든 게임 오브젝트의 최상위 부모 클래스입니다. 물리(Physics) 및 효과(Effect) 관리 시스템을 기본 제공합니다. 새로운 오브젝트는 이 클래스나 더 구체적인 하위 클래스(예: `BaseEnemy`)를 상속받아야 합니다.
-   **`BaseEnemy`**: 모든 적 유닛의 부모 클래스입니다. 데이터 기반으로 능력치, 외형, 공격 패턴을 설정하는 로직이 포함되어 있습니다.
-   **`BaseEffect`**: 모든 시각 효과의 부모 클래스입니다.

## 3. 관리자(Manager) 클래스 활용

게임의 주요 시스템은 각각의 관리자 클래스가 담당합니다.

-   **`EventManager` (`js/managers/EventManager.js`):** 게임의 여러 부분들이 서로 직접 참조하지 않고 소통할 수 있도록 하는 글로벌 이벤트 버스입니다. 시스템 간의 통신이 필요할 때는 이 `EventManager`를 통해 이벤트를 발생시키고(emit) 구독(on)하는 방식을 사용해야 합니다.
-   **`LevelManager` (`js/managers/LevelManager.js`):** `level_data.js`를 읽어 게임의 웨이브와 레벨 진행을 총괄합니다. 게임의 흐름과 관련된 로직은 이 클래스에서 관리합니다.
-   **`UIManager` (`js/managers/UIManager.js`):** 웨이브 시작 알림 등 모든 UI 관련 로직을 담당합니다.

## 4. 오브젝트 중심의 로직 (Entity-Centric Logic)

각 게임 오브젝트는 자신의 행동과 상태를 스스로 책임집니다.

-   **`GameScene`의 역할:** `GameScene.js`는 각 관리자(Manager)와 시스템을 초기화하고, 물리적 충돌과 같은 최상위 상호작용을 설정하는 역할만 합니다. 개별 오브젝트의 세부 로직을 `GameScene`의 `update`문에 추가하지 마십시오.
-   **자체 관리:** 예를 들어, `Tower`는 자신의 생성 애니메이션을 스스로 관리하며, `BaseEnemy`는 자신의 이동과 공격 로직을 `update` 메소드 안에서 직접 처리합니다. 새로운 기능을 추가할 때는 해당 기능이 속한 오브젝트의 클래스 내부에 구현하는 것을 원칙으로 합니다.

이 가이드라인을 따르면 코드의 일관성과 확장성을 유지할 수 있습니다.
